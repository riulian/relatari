<%= form_with(model: calitati) do |form| %>
  <% if calitati.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calitati.errors.count, "error") %> prohibited this calitati from being saved:</h2>

      <ul>
        <% calitati.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    
    <%= form.text_field :denumire, class: "form-control",placeholder:"Denumire"  %>
  </div>

  <div class="actions">
    <%= form.submit 'Adauga', class:"btn btn-secondary" %>
  </div>
<% end %>
